//Accept number of students from user. I need to give 5 apples to each student. How many apples are required?
#include<stdio.h>
int main(){
    int apples, A;
    printf("Enter The Number :");
    scanf("%d",&A);
    apples=A*5;
    printf("The apples are required :%d",apples);
}
